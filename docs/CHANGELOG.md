# 变更记录

记录规则：

- 最新版本放在最上面。
- 每次更新都记录新增、删除、变更和时间。
- 如果是破坏性变更，优先写清影响范围和替代用法。

## v0.1.5 - 2026-05-05

新增：

- 新增 `vr filings-text`，支持从本地 `filings` PDF/原始文件提取文本并缓存到 `data/raw/filings/<market>/<symbol>/texts/`。
- 项目引入 `pdfplumber`，用于本地年报/中报正文提取。
- 新增同仁堂国药 `03613` 的首份正式研究档案，包括初始深度分析报告和 thesis log。
- `investment-research/TODO.md` 新增两类框架待办：将霍华德·马克思的“防御性投资”原则正式纳入框架，以及梳理年报 PDF 阅读流程和工具组合。

变更：

- 修复港股/美股 `financials` 历史三表缓存去重逻辑：从仅按 `REPORT_DATE` 去重，改为按 `REPORT_DATE + 科目名` 去重，避免同一报告期只剩一条项目行。
- README 和 `investment-research-skill/SKILL.md` 明确个股分析的数据优先级：本地 `financials` -> 本地 `filings-text` -> 本地 `filings` 原始文件 -> 外部搜索。
- `filings-text` 提取时抑制底层 PDF 解析噪声，避免正常提取时输出大量无关 warning。

删除：

- 无。

备注：

- 此版本解决了 `financials --refresh` 后港股/美股本地 CSV 退化为“每年一条”的关键数据质量问题。

## v0.1.4 - 2026-05-05

新增：

- A 股研究表支持从本地财务摘要缓存计算长期指标：3/5 年营业总收入 CAGR、3/5 年净利润 CAGR、5 年净利润为正年数、5 年经营现金流为正年数、5 年经营现金流/净利润。
- README 新增 `investment-analysis` skill 的安装位置、非删除式同步方式、使用前数据准备和报告输出目录说明。

变更：

- A 股经营现金流长期指标沿用当前当期口径，通过 `每股经营现金流 × 估算股本` 计算；未引入完整现金流量表缓存，因此暂不新增严谨自由现金流稳定性指标。
- `docs/TODO.md` 将 A 股长期指标任务标记为已完成，并明确完成范围。

删除：

- 无。

## v0.1.3 - 2026-05-05

新增：

- 港股研究表和港股历史财报缓存默认跳过明显非经营主体，例如 ETF、杠杆/反向产品、权证和结构性证券。
- README 新增说明，解释为什么这类港股产品会被跳过或出现补充数据失败。

变更：

- 港股 `vr hk` 和 `vr financials --market hk` 现在会先过滤候选池，避免把明显不适合财务分析的产品继续送进增强指标流程。

删除：

- 无。

## v0.1.2 - 2026-05-05

新增：

- 支持本地黑名单：`uv run vr blacklist add/list/remove`。
- 研究表默认只打标黑名单，不会自动过滤公司。
- 研究 CSV 现在会增加 `黑名单` 和 `黑名单原因` 两列。
- README 新增研究表字段速查说明，帮助快速理解 CSV 里的核心列。

变更：

- README 新增黑名单使用说明和个股深度分析工作流补充。
- `docs/TODO.md` 新增黑名单功能完成记录。
- `docs/TODO.md` 将“示例输出字段说明”标记为已完成。

删除：

- 无。

## v0.1.1 - 2026-05-05

新增：

- 港股和美股研究表新增长期指标：3/5 年收入 CAGR、3/5 年净利润 CAGR、5 年净利润/经营现金流/自由现金流稳定性指标。
- README 新增长期指标说明，并说明这些指标依赖本地历史财报缓存。

变更：

- 港股研究表补齐 `净负债/EBITDA`、`利息保障倍数` 等字段别名。
- `hk/us` 研究表现在会优先用本地 `financials` 缓存计算长期指标，历史不够时留空。
- `docs/TODO.md` 将长期指标任务拆分为港美完成、A 股待补。

删除：

- 无。

## v0.1.0 - 2026-05-05

新增：

- 支持美股 `filings --market us` 缓存 SEC 申报索引。
- 支持美股 `filings --download` 仅保存索引和主文档。
- 支持 `financials --missing-only` 按文件补缺。
- 支持 `filings --workers` 并发跑公告任务。
- 新增 README 的快速上手、命令区别、工作流和常见问题说明。

变更：

- `financials` 默认仍然按股票增量追加，但 `--missing-only` 现在只补缺失的 statement 文件。
- `filings` 命令现在支持 `--workers`，默认保持保守串行。
- README 的术语统一为“公告索引 + 主文档”。
- 美股 filings 从“主文档 + 附件目录”收紧为“索引 + 主文档”。
- 财报和公告缓存默认只保留最近 5 年。

删除：

- 删除了旧的混杂式 README 叙述，改为命令导向的结构说明。
- 删除了美股 filings 的附件批量下载策略，只保留主文档下载。

备注：

- 当前版本记录从 `v0.1.0` 开始，后续每次更新请在顶部追加新版本。
